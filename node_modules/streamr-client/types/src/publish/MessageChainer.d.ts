import { MessageRef, MessageID } from 'streamr-client-protocol';
export default function MessageChainer({ streamId, streamPartition, publisherId, msgChainId }: {
    streamId: string;
    streamPartition: number;
    publisherId: string;
    msgChainId?: string;
}): (timestamp: number) => [MessageID, MessageRef | undefined];
