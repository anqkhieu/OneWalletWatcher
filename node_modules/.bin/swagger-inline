#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../swagger-inline/bin/swagger-inline" "$@"
  ret=$?
else 
  node  "$basedir/../swagger-inline/bin/swagger-inline" "$@"
  ret=$?
fi
exit $ret
