export declare class StreamPart {
    _streamId: string;
    _streamPartition: number;
    constructor(streamId: string, streamPartition: number);
    static fromStream({ id, partitions }: {
        id: string;
        partitions: number;
    }): StreamPart[];
    getStreamId(): string;
    getStreamPartition(): number;
}
