import { JsonRpcProvider } from '@ethersproject/providers';
declare type ProviderConnectionInfo = ConstructorParameters<typeof JsonRpcProvider>[0];
export declare type StorageNodeInfo = {
    address: string;
    url: string;
};
export declare class StorageNodeRegistry {
    private readonly records;
    constructor(records: StorageNodeInfo[]);
    getStorageNodeHTTP(address: string): string | never;
    getAllStorageNodes(): StorageNodeInfo[];
}
export declare function createStorageNodeRegistry(servers: StorageNodeInfo[]): StorageNodeRegistry;
export declare function getStorageNodeRegistryFromContract({ contractAddress, jsonRpcProvider }: {
    contractAddress: string;
    jsonRpcProvider: ProviderConnectionInfo;
}): Promise<StorageNodeRegistry>;
export {};
