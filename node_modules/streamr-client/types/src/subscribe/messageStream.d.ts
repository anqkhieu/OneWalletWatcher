import { ControlMessageType } from 'streamr-client-protocol';
import Connection from '../Connection';
import PushQueue from '../utils/PushQueue';
/**
 * Listen for matching stream messages on connection.
 * Returns a PushQueue that will fill with messages.
 */
export default function messageStream(connection: Connection, { streamId, streamPartition, isUnicast, type, }: {
    streamId: string;
    streamPartition: number;
    isUnicast: boolean;
    type?: ControlMessageType;
}, onFinally?: ((err?: Error) => void | Promise<void>)): PushQueue<never>;
export declare type MessageStream = ReturnType<typeof messageStream>;
