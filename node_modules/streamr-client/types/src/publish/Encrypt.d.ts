import { MessageLayer } from 'streamr-client-protocol';
import { Stream } from '../stream';
import { StreamrClient } from '../StreamrClient';
export default function Encrypt(client: StreamrClient): ((streamMessage: MessageLayer.StreamMessage, stream: Stream) => Promise<void>) & {
    setNextGroupKey(streamId: string, groupKey: import("../stream/encryption/Encryption").GroupKey): Promise<void>;
    rotateGroupKey(streamId: string): Promise<void>;
    rekey(streamId: string): Promise<void>;
    start(): Promise<void>;
    stop(): Promise<void>;
};
