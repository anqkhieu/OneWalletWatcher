import StreamMessage from '../protocol/message_layer/StreamMessage';
import OrderedMsgChain, { GapHandler, MessageHandler, MsgChainEmitter } from './OrderedMsgChain';
declare class OrderingUtil extends MsgChainEmitter {
    streamId: string;
    streamPartition: number;
    inOrderHandler: MessageHandler;
    gapHandler: GapHandler;
    propagationTimeout?: number;
    resendTimeout?: number;
    maxGapRequests?: number;
    orderedChains: {
        [key: string]: OrderedMsgChain;
    };
    constructor(streamId: string, streamPartition: number, inOrderHandler: MessageHandler, gapHandler: GapHandler, propagationTimeout?: number, resendTimeout?: number, maxGapRequests?: number);
    add(unorderedStreamMessage: StreamMessage): void;
    private getChain;
    markMessageExplicitly(streamMessage: StreamMessage): void;
    isEmpty(): boolean;
    clearGaps(): void;
    disable(): void;
}
export default OrderingUtil;
