import { EthereumAddress } from '../types';
export declare class StorageNode {
    static STREAMR_GERMANY: StorageNode;
    static STREAMR_DOCKER_DEV: StorageNode;
    private _address;
    constructor(address: EthereumAddress);
    getAddress(): string;
}
