import { StreamrClient } from '../StreamrClient';
import { EthereumConfig } from '../Config';
export declare function getAddressFromOptions({ ethereum, privateKey }?: {
    ethereum?: EthereumConfig;
    privateKey?: any;
}): Promise<string>;
export declare function getUserId(client: StreamrClient): Promise<string>;
