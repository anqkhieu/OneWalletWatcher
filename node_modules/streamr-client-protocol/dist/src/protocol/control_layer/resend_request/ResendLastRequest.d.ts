import ControlMessage, { ControlMessageOptions } from '../ControlMessage';
export interface Options extends ControlMessageOptions {
    streamId: string;
    streamPartition: number;
    numberLast: number;
    sessionToken: string | null;
}
export default class ResendLastRequest extends ControlMessage {
    streamId: string;
    streamPartition: number;
    numberLast: number;
    sessionToken: string | null;
    constructor({ version, requestId, streamId, streamPartition, numberLast, sessionToken }: Options);
}
