/// <reference types="node" />
export default class SigningUtil {
    static sign(payload: string, privateKey: string): Promise<string>;
    static recover(signature: string, payload: string, publicKeyBuffer?: Buffer | Uint8Array | undefined): Promise<string>;
    static verify(address: string, payload: string, signature: string): Promise<boolean>;
}
